# Secrets — never commit
api/meetup_secrets.json
api/wccentral_secrets.json
*.pem
*.key
*_private*

# OS / editor junk
.DS_Store
__pycache__/
*.pyc

# generated CSV export (not maintained by the refresh workflow)
meetup_groups.csv

# transient manual funnel-detail pull (see api/pull_funnel_detail.js)
funnel_detail.json
